hooks:
  unlink:
    - condition: always
      actions:
        - allow: false
  rmdir:
    - condition: always
      actions:
        - allow: false
  readdir:
    # - condition:
    #     not:
    #       file_content_contains:
    #         path: "checkpoint_2_answer.txt"
    #         expected_substring: "(?i)access_log\\.txt"
    #   actions:
    #     - value: ""
    - condition:
        not:
          file_content_contains:
            path: "checkpoint_2_answer.txt"
            expected_substring: "(?i)ECE_Display_Board/access_log\\.txt"
      actions:
        - value: ""

        
  getattr:
    # - condition:
      #   not:
      #     file_content_contains:
      #       path: "checkpoint_2_answer.txt"
      #       expected_substring: "(?i)access_log\\.txt"
      # actions:
      #   - value: null
    - condition:
        not:
          file_content_contains:
            path: "checkpoint_2_answer.txt"
            expected_substring: "(?i)ECE_Display_Board/access_log\\.txt"
      actions:
        - value: ""