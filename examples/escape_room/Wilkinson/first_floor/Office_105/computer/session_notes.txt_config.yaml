hooks:
  readdir:
    - condition:
        not:
          file_content_contains:
            path: "../terminal/game.puzzle"
            expected_substring: "access granted!"
      actions:
        - value: null

  getattr:
    - condition:
        not:
          file_content_contains:
            path: "../terminal/game.puzzle"
            expected_substring: "access granted!"
      actions:
        - error: no_exist

  access:
    - condition:
        not:
          file_content_contains:
            path: "../terminal/game.puzzle"
            expected_substring: "access granted!"
      actions:
        - error: access_denied